name: Greet Everyone
on: [push]

jobs:
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-dotnet@v1
      - name: publish nuget
        run: echo 'The time was ${{ steps.hello.outputs.time }}.'